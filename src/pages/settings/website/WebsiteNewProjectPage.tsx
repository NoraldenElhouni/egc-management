import NewProjectForm from "../../../components/website/NewProjectForm";

const WebsiteNewProjectPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 via-white to-gray-50 py-6 px-4 sm:px-6">
      <NewProjectForm />
    </div>
  );
};

export default WebsiteNewProjectPage;
